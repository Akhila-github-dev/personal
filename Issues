Null Pointer Exception: "A bug where the application tries to use an object reference that hasn't been initialized, causing the program to crash."

Memory Leak: "A bug where the application fails to release memory that is no longer needed, leading to increased memory usage and potential crashes."

Technical Issues: "We encountered a bug that caused unexpected behavior in the application. I tackled this by debugging systematically and consulting documentation and forums for solutions."
Bug Fixing: "We faced a bug that caused the application to crash under certain conditions. I had to debug the code, identify the root cause, and implement a fix."

Performance Optimization: "The application was running slower than expected. I analyzed the performance bottlenecks and optimized the code and database queries to improve speed."

Integration Problems: "Integrating a third-party service caused unexpected errors. I worked on understanding the API documentation better and adjusted our implementation to ensure compatibility."

Data Migration: "Migrating data from an old system to a new one led to data inconsistencies. I developed scripts to clean and validate the data during the migration process."

Security Vulnerabilities: "We discovered a security vulnerability in our application. I researched best practices and implemented security patches to protect user data."

Environment Configuration: "Setting up the development environment was challenging due to dependency conflicts. I documented the setup process and used containerization tools like Docker to streamline the environment configuration."\\asyn \compliated

=====================================================
✅ Low-level HTTP Client – Requires explicit configurations for headers, authentication, and serialization. ✅ Manual Handling – You have to manually create request bodies and parse responses. ✅ More Control – Allows fine-tuned customizations.
=============================================
The @Transactional annotation in Spring Boot is used to manage database transactions automatically. When applied to a method or class, it ensures that all database operations within the method are executed within a single transaction. If any exception occurs, the transaction is rolled back, ensuring data consistency. It simplifies transaction management in applications
=======================================================
1.what are the type of problems you faced in your project?
2. Is there any multithreading is used in you project?
3. tell about critical business logic you developed?
3. have you used microservices ? how many microservices you created?
===========================================================================
  List<List<Integer>> nestedList = Arrays.asList(
                Arrays.asList(1, 2, 3),
                Arrays.asList(4, 5, 6),
                Arrays.asList(7, 8, 9)
        );

        List<Integer> flattenedList = nestedList.stream()
                .flatMap(List::stream)
                .collect(Collectors.toList());
--------------------------------------------------------
API Integration: CashPro provides RESTful APIs for payments, account information, FX trading, and more, allowing businesses to automate financial operations.

Security & Compliance: It ensures secure file transmission and follows industry standards for data encryption and authentication.

Real-Time Processing: Supports instant payment validation and real-time treasury management, improving efficiency.

Scalability & Flexibility: Businesses can customize workflows and integrate CashPro APIs into their existing ERP or financial systems.

Multi-Channel Access: Offers web-based and mobile solutions, ensuring accessibility from various platforms.

In an interview, you could say: "CashPro Connect is a robust treasury management platform that provides API-driven 
solutions for businesses. As a backend Java developer, I would work on integrating its RESTful APIs for payments and
account management, ensuring secure transactions and real-time processing. My focus would be on optimizing API calls,
handling authentication, and ensuring seamless data flow between enterprise systems and CashPro."
=========================================================================================
Payment Service takes 30 seconds to respond. What will you do?

Answer

First, configure a timeout so the Order Service doesn't wait indefinitely.

Use a Circuit Breaker with Resilience4j to prevent repeated failures.

Configure Retry with a limited number of attempts.

If the Payment Service is unavailable, return a fallback response such as:

"Payment service is temporarily unavailable. Please try again later."

======================================================================================
| Scenario              | Status                        |
| --------------------- | ----------------------------- |
| Success               | **200 OK**                    |
| Created               | **201 Created**               |
| No Content (Delete)   | **204 No Content**            |
| Invalid Request       | **400 Bad Request**           |
| Unauthorized          | **401 Unauthorized**          |
| Forbidden             | **403 Forbidden**             |
| Resource Not Found    | **404 Not Found**             |
| Duplicate Resource    | **409 Conflict**              |
| Internal Server Error | **500 Internal Server Error** |
==========================================================================================
| Collection    | Order           | Sorted | Duplicate | Implementation           |
| ------------- | --------------- | ------ | --------- | ------------------------ |
| HashSet       | No              | No     | No        | Hash Table               |
| LinkedHashSet | Insertion Order | No     | No        | Hash Table + Linked List |
| TreeSet       | Sorted          | Yes    | No        | Red-Black Tree           |

Time Complexity
HashSet → O(1)
LinkedHashSet → O(1)
TreeSet → O(log n)

=================================================================================
Comparable is used for the natural ordering of objects and is implemented inside the class using the compareTo() method.

Comparator is used for custom sorting and is implemented in a separate class or as a lambda using the compare() method.

We use Comparable when there is only one default sorting order (for example, sorting employees by ID). We use Comparator when we need multiple sorting options, such as by salary, name, or age.

Easy Remember

Comparable → Inside the class → compareTo()
Comparator → Outside the class → compare()
=============================================================================================
Difference between ArrayList, Vector and CopyOnWriteArrayList.
Ideal Answer

ArrayList

Not thread-safe
Fast for read operations
Capacity grows by approximately 50% when full.

Vector

Thread-safe because methods are synchronized
Legacy collection
Capacity doubles (100%) by default when full.

CopyOnWriteArrayList

Thread-safe
On every write operation (add, remove, update), it creates a new copy of the underlying array.
Best when reads are much more frequent than writes.
Its iterator does not throw ConcurrentModificationException.
Question 2
Checked vs Unchecked Exception
Ideal Answer

Checked Exceptions

Checked at compile time.
Must be handled or declared using throws.
Examples:
IOException
SQLException
FileNotFoundException

Unchecked Exceptions

Occur at runtime.
Extend RuntimeException.
Examples:
NullPointerException
ArithmeticException
ArrayIndexOutOfBoundsException

We avoid catching generic Exception because it hides the real cause and makes debugging harder.

Correction: ClassNotFoundException is a checked exception, but in interviews, IOException and SQLException are more common examples.

Question 3
What is Spring AOP?
Ideal Answer

AOP (Aspect-Oriented Programming) separates business logic from cross-cutting concerns.

Common cross-cutting concerns include:

Logging
Security
Transaction Management
Performance Monitoring
Auditing

Common advice annotations:

@Before
@After
@AfterReturning
@AfterThrowing
@Around

We use AOP to avoid repeating the same code across multiple classes.

Question 4
Bean Scopes
Ideal Answer
Scope	Description
Singleton	One bean instance per Spring container (default).
Prototype	A new bean instance every time it is requested.
Request	One bean per HTTP request.
Session	One bean per HTTP session.
Application	One bean per ServletContext.

Correction: You mentioned header. The standard Spring bean scope is session, not "header."

Question 5
Request Validation
Ideal Answer

Use validation annotations:

@NotBlank
@Email
@Min(18)
@Max(100)

Controller:

@PostMapping
public ResponseEntity<Customer> save(
        @Valid @RequestBody CustomerRequest request)

Handle validation using @RestControllerAdvice.

Return:

HTTP Status: 400 Bad Request

Example response:

{
  "status": 400,
  "message": "Validation failed",
  "errors": [
    "Name must not be blank",
    "Email must be valid",
    "Age must be greater than 0"
  ]
}

Correction: You answered 405. The correct status is 400 Bad Request. 405 Method Not Allowed is used when the HTTP method itself
is not supported.

Question 6
Why is count++ not thread-safe?
Ideal Answer

count++ is actually three operations:

Read current value
Increment
Write new value

If two threads execute these steps simultaneously, updates can be lost.

Solutions:

synchronized
ReentrantLock
AtomicInteger
Difference

synchronized

Simple
JVM-managed
Automatically releases the lock

ReentrantLock

More flexible
Can use tryLock(), fairness, and interruptible locking
Must unlock manually

AtomicInteger

Lock-free
Uses compare-and-swap (CAS)
Very efficient for simple counters

Which would I choose?

For a simple counter → AtomicInteger
For complex critical sections → ReentrantLock or synchronized
Question 7
Customers who never placed an order

Your query was close. A complete version is:

SELECT c.*
FROM Customer c
LEFT JOIN Orders o
ON c.customer_id = o.customer_id
WHERE o.customer_id IS NULL;
WHERE vs HAVING
WHERE filters rows before grouping.
HAVING filters groups after GROUP BY.
Index

An index is a data structure that improves the speed of data retrieval by reducing the number of rows the database must scan.

Can indexes hurt performance?

Yes.

Because every INSERT, UPDATE, and DELETE must also update the index. Too many indexes slow down write operations and consume
additional storage.

Question 8
Learning a New Technology

Your answer was genuine, but here's a more structured version.

Question

How would you learn a technology you've never used before?

Ideal Answer

I would first understand the project requirements and identify the parts of the technology I need immediately. 
Then I would study the official documentation, build a small proof of concept, and seek guidance from experienced team members
when necessary. I would continue learning in parallel while delivering my assigned tasks, ensuring I become productive without
delaying the project. In my career, I've had to learn new frameworks and business domains quickly, and I've been able to adapt
successfully by following this approach.


Step 1: Customer Entity
public class Customer {

    private Long id;
    private String name;
    private String email;

    public Customer(Long id, String name, String email) {
        this.id = id;
        this.name = name;
        this.email = email;
    }

    public String getName() {
        return name;
    }

    public String getEmail() {
        return email;
    }
}
Step 2: Repository

Normally in Spring Boot:

Optional<Customer> findById(Long id);

Suppose it returns

Optional<Customer> customer =
Optional.of(new Customer(101L,"Akhila","akhila@gmail.com"));

Now let's see all the Optional methods.

1. isPresent()
if(customer.isPresent()) {
    System.out.println("Customer Found");
}

Output

Customer Found
Interview Answer

Checks whether Optional contains a value.

2. ifPresent()

Instead of writing

if(customer.isPresent()){
    System.out.println(customer.get().getName());
}

Write

customer.ifPresent(c ->
        System.out.println(c.getName()));

Output

Akhila
Interview Answer

Executes the lambda only if the value exists.

3. get()
Customer c = customer.get();

System.out.println(c.getEmail());

Output

akhila@gmail.com

⚠️ Never use

Optional.empty().get();

It throws

NoSuchElementException
4. orElse()

Suppose customer doesn't exist.

Optional<Customer> customer =
Optional.empty();

Customer c = customer.orElse(
        new Customer(0L,"Guest","guest@gmail.com"));

System.out.println(c.getName());

Output

Guest
Interview Answer

Returns the value if present; otherwise returns the default object.

5. orElseGet()
Customer c = customer.orElseGet(() ->
        new Customer(0L,"Generated User","guest@gmail.com"));

Difference:
orElse()
Always creates the object.

orElseGet()

Creates it only if needed.

6. orElseThrow()

This is the most common one in Spring Boot.

Customer c = repository.findById(id)
        .orElseThrow(() ->
        new RuntimeException("Customer Not Found"));

If customer doesn't exist

Output

RuntimeException:
Customer Not Found

This is exactly how we use it in projects.

7. map()

Suppose you only need the customer's email.

Without map

Customer c = customer.get();

System.out.println(c.getEmail());

Using map

Optional<String> email =
customer.map(Customer::getEmail);

System.out.println(email.get());

Output

akhila@gmail.com
Interview Answer

map() transforms the object inside the Optional into another value.

8. filter()

Suppose only Gmail users are allowed.

customer.filter(c ->
        c.getEmail().endsWith("@gmail.com"))
        .ifPresent(System.out::println);

If email is Gmail

Output

Customer object

If not Gmail

Nothing is printed.

9. of()
Optional<String> name =
Optional.of("Akhila");

Never do

Optional.of(null);

Throws

NullPointerException
10. ofNullable()
String email = null;

Optional<String> mail =
Optional.ofNullable(email);

Output

Optional.empty

No exception.

Complete Real-Time Example
Optional<Customer> customer =
repository.findById(101L);

//1
System.out.println(customer.isPresent());

//2
customer.ifPresent(c ->
System.out.println(c.getName()));

//3
Customer c1 =
customer.orElse(
new Customer(0L,"Guest","guest@gmail.com"));

//4
Customer c2 =
customer.orElseGet(()->
new Customer(0L,"Generated User","generated@gmail.com"));

//5
Customer c3 =
customer.orElseThrow(()->
new RuntimeException("Customer Not Found"));

//6
Optional<String> email =
customer.map(Customer::getEmail);

//7
customer.filter(c->
c.getEmail().endsWith("@gmail.com"))
.ifPresent(System.out::println);
How It Works (Flow)
Repository
      │
      ▼
Optional<Customer>
      │
      ├── isPresent() → true/false
      │
      ├── ifPresent() → Execute lambda
      │
      ├── get() → Return object
      │
      ├── orElse() → Default object
      │
      ├── orElseGet() → Lazily create object
      │
      ├── orElseThrow() → Throw exception
      │
      ├── map() → Convert Customer → Email
      │
      └── filter() → Apply condition
Easy Trick to Remember
Method	Purpose
of()	Create Optional (null not allowed)
ofNullable()	Create Optional (null allowed)
isPresent()	Check if value exists
ifPresent()	Execute code if value exists
get()	Get the value (avoid if possible)
orElse()	Return default value
orElseGet()	Lazily create default value
orElseThrow()	Throw exception if empty
map()	Transform the value
filter()	Apply a condition
💡 Interview Tip (Very Important)

If an interviewer asks:

"Have you used Optional in your project?"

You can confidently say:

"Yes. In our Spring Boot application, Spring Data JPA repository methods like findById() return Optional<Customer>.
In the service layer, we usually use orElseThrow() to throw a custom CustomerNotFoundException if the customer is not found.
Sometimes we use map() when we only need a specific field from the entity, and ifPresent() when an operation should execute only
if the value exists."

