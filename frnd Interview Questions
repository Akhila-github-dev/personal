IQGateway

JPQL query

		public void m1(int i,int j) {
			System.out.println(m1);
		}
		
		public void m2(double i,double j) {
			System.out.println(m2);
		}
		
		public static void main(String[] args) {
			System.out.println(m1(1.0,3.0));
			System.out.println(m1(1,3));
		}
output for above code

-------------------------
recursion,
Exception propagation
Exception propagation in Java means that when a method throws an exception and does not handle it (using try-catch), the exception is automatically passed to the calling method. 
This continues up the call stack until it is caught or reaches the JVM.
Propagation happens only for unchecked exceptions (like ArithmeticException) by default.
==================================================
instanceof -instanceof is a keyword used to check whether an object is an instance of a specific class or subclass. It's super handy for preventing ClassCastException errors!
classcastException -ClassCastException in Java happens when you try to cast an object to a class that it is not an instance of.

ClassCastException
When it happens: When you try to cast an object to an incompatible type
Type: Unchecked (Runtime exception)
Occurs at: Runtime

Example:

Object obj = "Hello";
Integer num = (Integer) obj; // ClassCastException
2. ClassNotFoundException
When it happens: When JVM cannot find a class during loading (usually with Class.forName())
Type: Checked exception
Occurs at: Compile-time requirement (must handle), but thrown at runtime

Example:

Class.forName("com.example.UnknownClass"); // ClassNotFoundException

| Feature  | ClassCastException   | ClassNotFoundException           |
| -------- | -------------------- | -------------------------------- |
| Cause    | Invalid type casting | Class not found in classpath     |
| Type     | Unchecked            | Checked                          |
| When     | Runtime              | During class loading             |
| Handling | Optional             | Must handle (try-catch / throws) |
==================================================
synchronous and asynchronous how to achieve
Synchronous

Tasks execute one after another
Each task waits for the previous one to finish
👉 Blocking behavior

Asynchronous

Tasks execute independently
No need to wait for previous task
👉 Non-blocking behavior

How to achieve in Java
1. Synchronous (default behavior)
Normal method calls
Single-thread execution
void process() {
    step1();
    step2(); // waits
}
2. Asynchronous ways
a) Using Thread
new Thread(() -> {
    System.out.println("Async task");
}).start();
b) Using ExecutorService (recommended)
ExecutorService executor = Executors.newFixedThreadPool(2);

executor.submit(() -> {
    System.out.println("Async task");
});

executor.shutdown();

“Synchronous execution is blocking and runs tasks sequentially, while asynchronous execution is non-blocking and can be achieved in Java using Threads, 
ExecutorService, or CompletableFuture.”
===========================================
unreachablecodeexception

class Car{

sysout(newCar());
output----hashcode

Concurrent hashmap vs hashmap
How can you ensure thread safety in concurrent hashmap
===============================
Static synchronous and asynchronous difference 
Types of synchronisation
1) Method-Level Synchronization
Entire method is locked
Only one thread can execute the method at a time
synchronized void print() {
    // critical section
}

👉 Lock is on the object (this)

2. Block-Level Synchronization
Only a specific block is locked (better performance)
void print() {
    synchronized(this) {
        // critical section
    }
}

👉 More efficient than method-level

3. Static Synchronization
Used for static methods
Lock is on the Class object
synchronized static void display() {
    // critical section
}

👉 Lock = ClassName.class

4. Class-Level Synchronization (Explicit)
Manually lock on class object
void show() {
    synchronized(ClassName.class) {
        // critical section
    }
}
“Synchronization in Java can be method-level, block-level, static, and class-level, depending on whether the lock is applied on an object or the class.”
=============================
Ioc container 
Types of dependency injection 
What is best

Custom exception 
Custom dependency 
2nd highest salary 
Max salary
Git commands
Solid principles 
Throwable class are interface 
Exceptions
Saga design pattern 
Saga ur using single db or multiple db
“We use the Saga pattern in a multi-database microservices architecture, where each service has its own database. Since we cannot use a distributed transaction across services,
Saga helps manage consistency by breaking the transaction into a sequence of local transactions with compensating actions in case of failure.”
===========================================
Resume chusi 40 % em chesavu annadu
Rest template and web client
================
Jre aa version lo remove indhi
Java 11
Java 11 introduced new String methods, a modern HTTP client, file API improvements, ability to run Java files directly, and removed standalone JRE and older Java EE modules
==============
Java 17 features 
1. Sealed Classes (Standardized)
Restrict which classes can extend/implement a class
public sealed class Shape permits Circle, Rectangle {}

2. Pattern Matching for instanceof
No need for explicit casting
if (obj instanceof String s) {
    System.out.println(s.length());
}

3. Switch Enhancements (Preview → Improved)
More concise and powerful switch
String result = switch (day) {
    case MONDAY, FRIDAY -> "Work";
    default -> "Other";
};
4. Records (Standardized)
Concise way to create immutable data classes
public record Person(String name, int age) {}

👉 Automatically gives constructor, getters, toString(), etc.

5. Text Blocks (Standardized)
Multi-line strings
String json = """
    {
        "name": "Akhila"
    }
    """;
6. New Garbage Collectors
ZGC and Shenandoah improved (low latency)
7. Strong Encapsulation of JDK Internals
Internal APIs are strongly hidden (better security)
8. Deprecation & Removal
Removed old/unused APIs (like Applet API deprecated)
🔥 Quick Summary
Sealed classes → controlled inheritance
Records → less boilerplate
Pattern matching → cleaner code
Text blocks → readable strings
Better GC + performance
🧠 One-line interview answer:

“Java 17 introduced sealed classes, records, pattern matching for instanceof, text blocks, and improved performance with enhanced garbage collectors.”
===================================
Project explanation
==========================
Tell me about yourself and projects you worked on.

2. What is ExecutorService in Java?

3. What is Virtual threads vs Platform Threads

4. How to secure your java application?

5. Compare Jwt and OAuth token and what will you prefer to use?

6. What is difference between using Volatile vs Synchronised.

7. Write a program to count the characters from given String s1 ="This is my Java Program"

8. Let's say if you are working on production banking application and there is slight delay in loading application, how will you debug it?

9. Write a program to print even numbers and multiply by 5 using java8 stream.
